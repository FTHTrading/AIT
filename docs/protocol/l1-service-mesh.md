# L1 Service Mesh

The AIT stack treats UnyKorn L1 and the service mesh as downstream infrastructure for state anchoring, module registration, and event distribution.

Current scope:

- Unsigned anchor payload generation
- Module and event namespace planning
- Future broadcast hooks after review and signing

Out of scope today:

- Live chain writes by default
- Unreviewed auto-broadcast
- Public disclosure of restricted material
